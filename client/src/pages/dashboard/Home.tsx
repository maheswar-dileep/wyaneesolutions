import { Button } from '../../components/ui/button';

const Home = () => {
    return (
        <div>
            <Button variant="outline">Demo</Button>
        </div>
    );
};

export default Home;
